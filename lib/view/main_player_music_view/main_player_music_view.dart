import 'package:flutter/material.dart';
class MainPlayerMusicView extends StatelessWidget {
  const MainPlayerMusicView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
